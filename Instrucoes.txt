python manage.py runserver 0.0.0.0:8500

Username: inspector
Password: inspectorpassword

python -m venv venv 
venv\Scripts\activate

python -m pip install --upgrade pip
pip install -r requirements.txt --trusted-host pypi.org --trusted-host files.pythonhosted.org

# 2. Executa as migrações e cria as tabelas de fato no banco
python manage.py migrate

python manage.py collectstatic

python manage.py createsuperuser

7. CONTROLE DE VERSÃO COM GIT E GITHUB
--------------------------------------------------------------------------------
Para vincular este projeto local ao seu repositório do GitHub e realizar o controle 
de alterações a cada melhoria realizada, utilize os comandos abaixo no terminal 
(certifique-se de ter o Git instalado no seu PC):

A. Configuração Inicial (Executar apenas na primeira vez):
---------------------------------------------------------
1. Inicializar o repositório Git local na pasta do projeto:
   > git init

2. Vincular este repositório local ao seu GitHub remoto:
   > git remote add origin https://github.com/programasindustriais13/sinotipo_vulcanizacao.git

3. Configurar sua identidade no Git (caso ainda não tenha configurado no seu PC):
   > git config --global user.name "Seu Nome"
   > git config --global user.email "seu_email@exemplo.com"

4. Adicionar todos os arquivos ao controle de versão (o arquivo `.gitignore` que 
   criei impedirá automaticamente o envio de arquivos temporários, da pasta `venv/` 
   e do banco local de testes `db.sqlite3` para a nuvem):
   > git add .

5. Realizar o primeiro commit de registro:
   > git commit -m "Commit inicial: Estrutura do Painel Sinoptico Django para TV 43"

6. Definir o nome da branch principal como main:
   > git branch -M main

7. Enviar o código pela primeira vez para o GitHub:
   > git push -u origin main

B. Rotina diária de melhorias (A cada nova alteração que você fizer):
-------------------------------------------------------------------
Sempre que você fizer alterações nos templates, views ou scripts e quiser subir
a nova versão para o GitHub:

1. Adicione os arquivos alterados:
   > git add .

2. Faça o commit descrevendo o que foi implementado:
   > git commit -m "Melhoria: adicionado Regra de não selecionar maquina em aprovação"

3. Envie para o GitHub:
   > git push origin main

================================================================================
ALTERAÇÃO: Correção Card Aprovados + Filtro por Período na Dashboard
Data: 2026-06-22
================================================================================

Arquivos alterados:
- core/views.py
- core/templates/core/dashboard.html

Resumo das alterações:

1. CORREÇÃO DO CARD "APROVADOS" (BUG):
   - O card usava {% regroup %} no template para contar aprovados, o que podia
     exibir valor vazio ao invés de "0" quando não havia aprovados.
   - Lógica movida para a view (backend): approved_count calculated via
     sessions.filter(status='APROVADO').count()
   - Template agora exibe apenas {{ approved_count }}

2. FILTRO POR PERÍODO NA DASHBOARD:
   - Adicionado formulário GET com campos Data Inicial e Data Final + botão Filtrar
   - Default: mês corrente (1º dia até hoje)
   - Filtra: cards indicadores (Total, Aprovados, Ativos, NCs) e tabela de histórico
   - NÃO filtra: filas de aprovação (Líder/Analista/Correções) — operacionais
   - Botão "Limpar" volta ao filtro padrão (mês corrente)

3. EXPORTAÇÃO EXCEL COM FILTRO:
   - Botão "Exportar Histórico (Excel)" passa data_inicio e data_fim na URL
   - View export_dashboard_excel respects the filter of period
   - Nome do arquivo inclui o período: Historico_Checklists_DD-MM-YYYY_a_DD-MM-YYYY.xlsx

Nenhuma migration necessária (sem alteração de models).
Testes automatizados: 11/11 OK.


================================================================================
ALTERAÇÃO: Criação do Dashboard Analítico (Painel Gráfico) e Refatoração do Menu
Data: 2026-06-22
================================================================================

Arquivos alterados/criados:
- core/urls.py
- core/views.py
- core/templates/core/base.html
- core/templates/core/analytical_dashboard.html [NEW]
- core/tests.py

Resumo das alterações:

1. ROTA E VISUALIZAÇÃO:
   - Adicionada rota '/dashboard/' mapeando para views.analytical_dashboard com nome 'analytical_dashboard'.
   - Adicionada view analytical_dashboard restrita via @login_required e @machine_management_required (Analistas, Diretores e Superusers).
   - A view herda o filtro de período e calcula os dados para os 4 gráficos utilizando agrupamento em Python para total compatibilidade SQLite/MySQL.

2. ATUALIZAÇÕES DO MENU (UI):
   - Renomeado o link "Dashboard" atual para "Início" no template base.html.
   - Adicionado novo link "Dashboard Analítico" visível apenas a perfis autorizados (Analista, Diretor e Superuser).

3. COMPONENTE GRÁFICO (TEMPLATE):
   - Criado o template analytical_dashboard.html herdando base.html.
   - Utilização de Chart.js via CDN para plotagem dos 4 gráficos:
     * 001. Evolução de Checklists Realizados (Linha/Tendência - agrupamento dinâmico por dia, semana ou mês com base na amplitude do período).
     * 002. Quantidade de Problemas Encontrados por Máquina (Barras, ordenado decrescente).
     * 003. Distribuição dos Tipos de Falha (Donut, com cores harmoniosas).
     * 004. Reincidência de Problemas (Barras Empilhadas por Mês e Seção).
   - Integração completa com o formulário de Filtro de Período, atualizando os gráficos dinamicamente.
   - Adicionado tratamento para gráficos sem dados no período selecionado (exibição de overlay informativo de estado vazio).

4. TESTES E VALIDAÇÃO:
   - Adizados testes automatizados cobrindo RBAC de acesso (bloqueio de mecânicos/operadores, liberação de analistas), presença de gráficos no HTML e passagem do filtro de período.
   - Testes automatizados: 12/12 OK.


================================================================================
ALTERAÇÃO: Bloqueio de Máquina já Inspecionada Pendente de Aprovação do Líder
Data: 2026-06-22
================================================================================

Arquivos alterados:
- core/forms.py
- core/views.py
- core/tests.py

Resumo das alterações:

1. FILTRAGEM DO DROPDOWN NO FRONTEND (FORM):
   - Modificado o formulário `ChecklistStartForm` em `core/forms.py` para receber opcionalmente o parâmetro `user` no `__init__`.
   - Caso `user` seja fornecido, o queryset da máquina continua contendo todas as máquinas (para validação do POST), mas a propriedade `choices` do campo de seleção é filtrada para excluir as máquinas que possuam checklists pendentes de aprovação (`AGUARDANDO_LIDER` ou `REPROVADO_LIDER`) criados pelo próprio inspetor.

2. VALIDAÇÃO DE SEGURANÇA NO BACKEND (FORM CLEAN):
   - Adicionada validação no método `clean()` do `ChecklistStartForm` para verificar se a máquina submetida possui algum checklist pendente de aprovação feito pelo próprio inspetor.
   - Caso possua, gera um erro de validação impedindo a criação do checklist com a mensagem: *"Você já realizou uma inspeção nesta máquina que está aguardando a aprovação do Líder."*

3. INSTANCIAÇÃO NA VIEW (VIEWS):
   - A view `checklist_start` em `core/views.py` foi atualizada para instanciar o formulário passando o `user=request.user` tanto no método GET quanto no POST.

4. TESTES DE INTEGRAÇÃO E RBAC (TESTS):
   - Adicionado o teste `test_block_inspected_machine_selection_pending_approval` em `core/tests.py` cobrindo o bloqueio na listagem de choices, a rejeição com erro customizado no POST de máquina bloqueada, liberação de máquina após aprovação, restrição após rejeição do líder e permissão de acesso para outros inspetores.
   - Testes automatizados: 13/13 OK.

================================================================================
ALTERAÇÃO: Filtros Avançados, Campo de Observação Geral e Modal Responsivo
Data: 2026-06-23
================================================================================

Arquivos alterados/criados:
- core/models.py
- core/views.py
- core/templates/core/dashboard.html
- core/templates/core/checklist_form.html
- core/static/core/style.css
- core/tests.py
- core/migrations/0005_checklistsession_general_observations.py [NEW]

Resumo das alterações:

1. BANCO DE DADOS (MODELS E MIGRATIONS):
   - Adicionado o campo `general_observations` (TextField, opcional) na classe `ChecklistSession` em `core/models.py`.
   - Gerada e aplicada migration incremental (`0005_checklistsession_general_observations.py`).

2. EXECUÇÃO DE CHECKLIST E SALVAMENTO (VIEWS E FORMS):
   - Atualizada a view `checklist_execute` para salvar as observações gerais tanto na pausa (`action=pause`) quanto na finalização (`action=finalize`).
   - Adicionada a área de "Estado Geral da Máquina" ao final do formulário `checklist_form.html`. A área é somente leitura (`readonly`) se o checklist não estiver `IN_PROGRESS`.

3. FILTROS AVANÇADOS NA DASHBOARD (HTML E VIEWS):
   - Expandido o formulário de filtros do Dashboard (`dashboard.html`) para abranger: Data (Início/Fim), Máquina (dropdown), Inspetor (dropdown), Líder (dropdown), Status (dropdown) e NC (busca textual "Resumo da NC").
   - Atualizada a view `dashboard` para filtrar a queryset combinando todos os parâmetros de busca fornecidos via GET e evitando duplicidade de linhas usando `.distinct()`.
   - O link de exportação consolidada do histórico repassa todos os filtros ativos na URL.

4. EXPORTAÇÃO EXCEL ATUALIZADA (VIEWS):
   - `export_dashboard_excel`: Atualizada para respeitar os filtros combinados aplicados e incluir a 9ª coluna "Observações Gerais".
   - `export_checklist_excel`: Inclui as "Obs. Gerais da Máquina" mesclada abaixo do Histórico de Pausas no bloco de metadados.

5. MODAL DE OBSERVAÇÕES RESPONSIVO (MOBILE):
   - Criado modal Bootstrap `#observationModal` com a classe `modal-fullscreen-sm-down` em `checklist_form.html`.
   - Adicionado botão de expandir observação (`.btn-obs-modal`) ao lado da textarea de cada item.
   - Implementado script em JS para sincronização dinâmica de leitura/escrita e controle de readonly entre o modal e as textareas correspondentes.
   - Adicionada media query customizada no `style.css` definindo `width: 95%` para o modal em viewports menores que 576px.

6. TESTES E VALIDAÇÃO:
   - Adizados testes automatizados cobrindo a persistência do campo de observações gerais na pausa/finalização, exibição no HTML, exportações Excel e validação dos filtros combinados.
   - Testes automatizados: 15/15 OK.

================================================================================
ALTERAÇÃO: Expiração Absoluta de Sessão (2h) e Pausa Automática de Checklist
Data: 2026-06-23
================================================================================

Arquivos alterados:
- checklist_system/settings.py
- checklist_system/urls.py
- core/views.py
- core/templates/core/base.html
- core/tests.py

Resumo das alterações:

1. CONFIGURAÇÕES GLOBAIS DE SESSÃO (BACKEND):
   - Desativado o pacote `django-session-security` removendo seu middleware e das `INSTALLED_APPS`.
   - Configurada a expiração de sessão nativa do Django no `settings.py`:
     * `SESSION_COOKIE_AGE = 7200` (Sessão dura exatamente 2 horas).
     * `SESSION_SAVE_EVERY_REQUEST = False` (Evita renovar a expiração a cada clique, tornando-a absoluta).

2. ROTAS E ENDPOINTS:
   - Removido o redirecionamento/URL de `session_security/` do `urls.py` do projeto.

3. LOG E AUDITORIA (VIEWS):
   - Atualizada a justificativa automática registrada em `ChecklistTimelineLog` ao pausar via timeout para: `"Pausa automática: Fim do tempo de sessão logada (2h)"`.

4. INTERFACE E SCRIPTS (FRONTEND):
   - Removidos os scripts antigos e modais de inatividade baseados em mouse/toques em `base.html`.
   - Implementado um banner de contagem regressiva flutuante e discreto (canto inferior direito) que aparece somente nos últimos 5 minutos de sessão.
   - Implementada lógica JavaScript que rastreia os segundos restantes (`request.session.get_expiry_age`), calculando o término de forma imune a drifts/atrasos do clock do navegador.
   - 10 segundos antes do limite absoluto da sessão terminar (para evitar erros de sessão já expirada no servidor), o script dispara automaticamente uma requisição AJAX `/timeout-pause/` e faz o logout forçado do usuário.

5. TESTES E VALIDAÇÃO:
   - Adicionado teste automatizado `test_timeout_pause_checklist` para validar o comportamento da view de pausa e a geração da justificativa de 2h na timeline.
   - Testes automatizados: 16/16 OK.


================================================================================
ALTERAÇÃO: Visibilidade Restrita de Checklists e Sistema de Duplas de Turno
Data: 2026-06-23
================================================================================

Arquivos alterados/criados:
- core/models.py
- core/views.py
- core/admin.py
- core/templates/core/dashboard.html
- core/templates/core/checklist_form.html
- core/tests.py
- core/migrations/0006_checklistsession_co_inspector_user_partner_user.py [NEW]

Resumo das alterações:

1. BANCO DE DADOS (MODELS & MIGRATIONS):
   - Adicionado o campo `partner_user` (ForeignKey para self, SET_NULL, null=True, blank=True) na classe `User` em `core/models.py`.
   - Adicionado o campo `co_inspector` (ForeignKey para User, SET_NULL, null=True, blank=True) na classe `ChecklistSession` em `core/models.py`.
   - Implementado método `save()` customizado em `User` para garantir que o relacionamento de dupla seja simétrico (se A aponta para B, B aponta para A) e se auto-limpe se desfeito.
   - Gerada e aplicada migration incremental (`0006_checklistsession_co_inspector_user_partner_user.py`).

2. DJANGO ADMIN (ADMIN):
   - Atualizado `CustomUserAdmin` para expor o campo `partner_user`.
   - Sobrescrito o método `get_form` para que a listagem de escolhas de `partner_user` mostre apenas técnicos ativos (Eletricista, Mecânico, Eletromecânico) e exclua o usuário atual sob edição.
   - Atualizado `ChecklistSessionAdmin` para exibir a coluna `co_inspector` nas listagens e filtros.

3. LOGICA DE NEGOCIO E VISIBILIDADE (VIEWS):
   - `dashboard`: Interceptada a QuerySet principal para usuários operacionais (técnicos), aplicando o filtro rigoroso `Q(inspector=user) | Q(co_inspector=user)` para garantir que eles visualizem apenas os próprios checklists e os de sua dupla. A fila de correções pendentes (`pending_corrections`) foi ajustada de forma idêntica.
   - `checklist_start`: Ao iniciar uma sessão, se o inspetor atual possuir um `partner_user` ativo configurado no Admin, ele é atribuído automaticamente ao campo `co_inspector` da sessão.
   - `checklist_execute` & `checklist_reopen`: Ajustadas as verificações de permissão para permitir que tanto o inspetor quanto o co-inspetor (dupla) possam executar, editar e reabrir o checklist de turno.

4. EXPORTAÇÃO EXCEL ATUALIZADA (VIEWS):
   - `export_checklist_excel`: Atualizada para concatenar os nomes e especialidades de ambos os inspetores no campo "Inspetor(es)" se houver co-inspetor cadastrado.
   - `export_dashboard_excel`: Inserida uma nova coluna "Co-Inspetor" logo ao lado de "Inspetor" no relatório consolidado, listando nome e especialidade do parceiro de turno.

5. INTERFACE (HTML TEMPLATES):
   - `checklist_form.html`: Modificado o cabeçalho de execução para exibir "INSPETORES" com nomes e especialidades de ambos no formato `"João (Mecânico) & Paulo (Eletricista)"` caso haja co-inspetor cadastrado.
   - `dashboard.html`: Modificadas todas as tabelas (histórico principal, pendências de Líder e Analista) para concatenar ou renderizar os dados do inspetor e co-inspetor conjuntamente.

6. TESTES E VALIDAÇÃO:
   - Adicionados testes automatizados cobrindo o relacionamento simétrico no banco, preenchimento automático do co-inspetor, isolamento de visualização de QuerySet de acordo com o perfil/dupla, permissão de edição para co-inspetores (e proibição para terceiros) e exportações de Excel.
   - Testes automatizados: 21/21 OK.


================================================================================
ALTERAÇÃO: Criação do Perfil de Auditor e Camada de Auditoria Gerencial
Data: 2026-06-30
================================================================================

Arquivos alterados/criados:
- core/models.py
- core/views.py
- core/urls.py
- core/templates/core/base.html
- core/templates/core/auditor_queue.html [NEW]
- core/templates/core/checklist_audit.html [NEW]
- core/templates/core/auditor_history.html [NEW]
- core/tests.py
- core/migrations/0007_checklistitemvalue_auditor_is_divergent_and_more.py [NEW]

Resumo das alterações:

1. BANCO DE DADOS (MODELS & MIGRATIONS):
   - Adicionada especialidade/perfil 'Auditor' no model User.
   - Adicionados campos `audit_status`, `audited_by` (FK para User) e `audited_at` (DateTimeField) no model ChecklistSession.
   - Adicionados campos `auditor_is_divergent` (BooleanField) e `auditor_observation` (TextField) no model ChecklistItemValue.
   - Gerada e aplicada migration incremental (`0007_checklistitemvalue_auditor_is_divergent_and_more.py`).

2. RESTRIÇÕES DE ACESSO (RBAC & DECORATORS):
   - Criado decorator `auditor_or_management_required` restrito para Auditores, Analistas, Diretores e Superusers.
   - Atualizada view `dashboard` para redirecionar usuários com specialty 'Auditor' diretamente para `/auditor/queue/`.
   - Bloqueado o Auditor de acessar rotas restritas de máquinas, criação de checklists e aprovação de usuários.
   - Bloqueado o acesso de técnicos e líderes às telas de auditoria (Fila geral, Execução e Histórico).

3. TELAS DA AUDITORIA (VIEWS E TEMPLATES):
   - `auditor_queue`: Fila geral exibindo todos os checklists do sistema, com filtros avançados por Data, Máquina, Status operacional e Técnico.
   - `checklist_audit`: Tela com dados originais em somente leitura. Permite ao Auditor apontar divergências em cada item com observação obrigatória integrada via JavaScript e validada no backend.
   - `auditor_history`: Histórico contendo os checklists já auditados e o resumo das divergências registradas.

4. EXPORTAÇÃO EXCEL ATUALIZADA (VIEWS):
   - `export_checklist_excel`: Exibe os metadados de auditoria e colunas "Divergência (Auditor)" e "Observação (Auditor)".
   - `export_dashboard_excel`: Inclui colunas "Status da Auditoria", "Auditor Responsável" e "Observações da Auditoria" no relatório consolidado.

5. TESTES E VALIDAÇÃO:
   - Adicionados testes automatizados cobrindo o controle de acessos do Auditor, isolamento de rotas e preenchimento de campos obrigatórios.


================================================================================
ALTERAÇÃO: Refatoração de Layout (Sidebar) e Identidade Visual (Pneus Freedom)
Data: 2026-06-30
================================================================================

Arquivos alterados:
- checklist_system/settings.py
- checklist_system/urls.py
- core/static/core/style.css
- core/templates/core/base.html
- core/templates/core/login.html
- core/views.py
- core/tests.py

Resumo das alterações:

1. CONFIGURAÇÃO DE MÍDIA (BACKEND):
   - Adicionada configuração de MEDIA_URL e MEDIA_ROOT no settings.py.
   - Adicionado roteamento re_path para servir arquivos de mídia de forma incondicional em urls.py, usando django.views.static.serve.

2. PALETA DE CORES CORPORATIVA E CSS (FRONTEND):
   - Atualizados estilos globais no style.css para usar a paleta corporativa da Pneus Freedom (Amarelo #eac532 como primária e tons escuros/preto como secundária).
   - Inseridas classes CSS customizadas para Sidebar responsiva (incluindo estados hover, links ativos, e table headers com fundo amarelo e texto preto para legibilidade).

3. MENU DE NAVEGAÇÃO LATERAL (SIDEBAR):
   - Substituída a barra de navegação superior (navbar) em base.html por uma barra lateral (Sidebar) esquerda integrada usando offcanvas responsivo do Bootstrap 5.
   - A Sidebar renderiza estaticamente em telas grandes (Desktop) e se transforma em menu hambúrguer com gaveta offcanvas em smartphones/tablets em modo retrato.
   - Links e acessos configurados dinamicamente com base nas permissões e especialidades do usuário (RBAC).

4. INTEGRAÇÃO DE LOGOS E DESIGN CORPORATIVO:
   - Tela de login atualizada para renderizar a logomarca preta (Untitled-1.png) centralizada acima do formulário de acesso.
   - Sidebar configurada com a logomarca amarela/dourada (Untitled-2.png) no topo sob fundo escuro.
   - Atualizadas as exportações em Excel (export_checklist_excel e export_dashboard_excel) para incluir a logo da empresa (Untitled-2.png) na célula A1, definir altura do cabeçalho de 60px e aplicar o design visual (cabeçalho preto com título amarelo e colunas com fundo amarelo e texto preto).

5. AUTOMATIZAÇÃO DE TESTES (QA):
   - Adicionados testes automatizados test_media_urls_serving (validação do carregamento de logos) e test_excel_exports_branding_and_logos (validação das saídas Excel com as formatações de logo/células).
   - Todos os 26 testes da aplicação passam com sucesso (26/26 OK).


================================================================================
ALTERAÇÃO: Correções de Usabilidade no Sidebar (Desktop e Mobile)
Data: 2026-06-30
================================================================================

Arquivos alterados:
- core/templates/core/base.html
- core/static/core/style.css

Resumo das alterações:

1. CORREÇÃO DA ALTURA E SAIR DO SIDEBAR (DESKTOP):
   - Sidebar configurada com altura fixa (100vh) e fixada à esquerda (position: fixed) usando a classe `.sidebar-fixed-desktop`.
   - Adicionada rolagem vertical isolada (overflow-y: auto) para a lista de navegação `.sidebar-nav`, permitindo que os links rolem independentemente se necessário.
   - O rodapé da sidebar contendo o botão "Sair do Sistema" foi fixado no canto inferior (usando mt-auto em layout flexbox), mantendo-o visível e acessível o tempo todo.
   - Adicionado deslocamento de margem (margin-left: 280px) para a área de conteúdo principal `.main-content-wrapper` em resoluções >= 768px.

2. BOTÃO FLUTUANTE DE MENU (MOBILE):
   - O botão que abre a sidebar (hambúrguer) foi retirado da barra do topo e posicionado como um elemento flutuante (position: fixed, top: 8px, left: 15px) na classe `.mobile-menu-trigger`.
   - Cabeçalho mobile (Mobile Top Header Bar) simplificado, mantendo apenas a logomarca e título centralizados.
   - Adicionado z-index elevado (1050) e semi-transparência (opacity: 0.6) para evitar bloqueio visual, com efeito de transição de opacidade total (1.0) e inversão de cores no hover/touch/focus.

3. TESTES E VALIDAÇÃO (QA):
   - Execução completa da suite de testes automatizados com sucesso (26/26 OK).


================================================================================
ALTERAÇÃO: Correção de Layout - Sobreposição do Botão e Logo no Sidebar (Desktop)
Data: 2026-06-30
================================================================================

Arquivos alterados:
- core/templates/core/base.html
- core/static/core/style.css

Resumo das alterações:

1. CORREÇÃO DA VISIBILIDADE DO BOTÃO DE MENU (DESKTOP):
   - Ocultado o botão flutuante `.mobile-menu-trigger` em telas desktop (resoluções >= 768px) no CSS via `@media (min-width: 768px) { .mobile-menu-trigger { display: none !important; } }` para corrigir o bug de sobreposição da logo.
   - Reestruturado o cabeçalho do sidebar desktop (`sidebar-header-brand`) em base.html para utilizar Flexbox (`d-flex justify-content-between align-items-center`) e adicionado o botão de menu (hambúrguer) lado a lado com a logo e nome da marca.
   - Adicionada estilização e animação de hover para o novo botão de menu desktop.

2. TESTES E VALIDAÇÃO (QA):
   - Suite de testes Django executada e todos os 26 testes passaram com sucesso (26/26 OK).


================================================================================
ALTERAÇÃO: Reposicionamento do Botão de Menu e Ocultação do Trigger Mobile no Desktop
Data: 2026-06-30
================================================================================

Arquivos alterados:
- core/templates/core/base.html
- core/static/core/style.css

Resumo das alterações:

1. OCULTAÇÃO DO TRIGGER MOBILE NO DESKTOP:
   - Isolados os estilos de `.mobile-menu-trigger` dentro de uma media query `@media (max-width: 767.98px)` no arquivo `style.css`, garantindo que o botão flutuante redondo só apareça em dispositivos móveis e seja ocultado no Desktop.
   - Adicionada a tag de cache buster `?v=1.0.1` ao stylesheet de `style.css` no template `base.html` para forçar o recarregamento dos estilos modificados pelo navegador.

2. REPOSICIONAMENTO E ESTILIZAÇÃO DO BOTÃO DE MENU (DESKTOP):
   - Mapeados e unificados os estilos do botão hambúrguer no Desktop dentro de `.desktop-menu-trigger` em `style.css` (para telas >= 768px). Ele agora exibe o mesmo formato circular premium (fundo preto, borda e ícone amarelos, transição suave de opacidade e hover amarelo com ícone preto).
   - O botão circular foi movido para o canto superior direito da sidebar no Desktop, ocupando o lugar do ícone hambúrguer solto secundário, liberando o canto esquerdo da sidebar para a logomarca da Pneus Freedom, eliminando sobreposições.
   - Limpadas as classes utilitárias e estilos inline obsoletos do botão e ícone no HTML de `base.html`.

3. TESTES E VALIDAÇÃO:
   - Validação automatizada realizada com sucesso (26/26 testes Django aprovados).
   - Validação visual realizada no Desktop e no Mobile via subagente de navegação para certificar de que não houve regressões responsivas no Mobile.




